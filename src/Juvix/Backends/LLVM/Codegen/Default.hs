-- | This module provides the default environment to run LLVM code in
module Juvix.Backends.LLVM.Codegen.Default where

import Juvix.Library

-- TODO ∷ create the environment to actually run LLVM in
-- TODO ∷ Figure out how to call functions across modules

defaultEnv = undefined
