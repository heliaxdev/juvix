mod Fail where

open Prelude

open Michelson

sig main : pairTy int int -> pairTy (list operation) int
let main = \pair ->
  cons-pair
    nil
    (if2 (compare 0 (car pair))
         (fail-with 0)
         (cdr pair))