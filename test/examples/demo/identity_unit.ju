open Prelude
open Prelude.Michelson

sig storage : ty
let storage = unit-t

sig param : ty
let param = unit-t

sig default : param -> storage -> pair storage (list operation)
let default _ storage = cons-pair storage nil
