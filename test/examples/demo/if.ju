mod If where

open Prelude

open Michelson

sig main : pairTy int int -> pairTy (list operation) int
let main = \pair ->
  cons-pair
    nil
    (if2 (compare 0 (car pair))
         (cdr pair)
         (add (car pair) (cdr pair)))